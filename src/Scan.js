export default class Scan {
    constructor(name, elevationMax, elevationMin, userId, userName) {
        this.name = name;
        this.elevationMax = elevationMax;
        this.elevationMin = elevationMin;
        this.userId = userId;
        this.userName = userName;
    }
}